# Архитектура b24-doc-gen

Документ описывает структуру проекта — монорепо, модули, файлы и их
назначение. Обновляется при каждом добавлении/удалении файла, функции
или класса.

## Обзор

`b24-doc-gen` — локальное приложение для Bitrix24, которое встраивается
в карточку сделки через `PLACEMENT_BIND` и позволяет:

- **Админам** загружать `.docx`-шаблоны с плейсхолдерами `{tagKey}` /
  `{fieldKey}` / `{#products}…{/products}` / `{%image}`, расставленными в
  Word, и в редакторе привязывать к этим тегам формулы (на основе полей
  сделки/контакта/компании) и ручные поля. Форматирование живёт в самом
  `.docx` — генерация подставляет значения прямо в оригинал
  (`buildDocxFromTemplate`), сохраняя верстку 1:1.
- **Пользователям** выбирать шаблон, видеть preview подставленного `.docx`
  (рендер `docx-preview` в браузере), генерировать `.docx` и привязывать
  его к сделке.

Разметка шаблонов и миграция существующих документов описаны в
[`docs/template-markup-guide.md`](./docs/template-markup-guide.md).

Архитектурно проект — это pnpm monorepo с тремя пакетами:

- [`apps/frontend`](./apps/frontend) — React SPA (Vite)
- [`apps/backend`](./apps/backend) — Fastify REST API + Prisma/SQLite
- [`packages/shared`](./packages/shared) — общие TypeScript-типы

## Корневые файлы

| Файл | Назначение |
|---|---|
| [`package.json`](./package.json) | pnpm workspace root. Общие скрипты (`dev`, `build`, `typecheck`), devDependencies (TypeScript, Prettier). |
| [`pnpm-workspace.yaml`](./pnpm-workspace.yaml) | Декларация workspace: `apps/*`, `packages/*`. |
| [`tsconfig.base.json`](./tsconfig.base.json) | Базовый TS-конфиг, который наследуют `apps/frontend`, `apps/backend`, `packages/shared`. Target ES2022, strict: true, ESM. |
| [`.gitignore`](./.gitignore) | Игнор `node_modules`, `dist`, `.env`, `*.db`. |
| [`.npmrc`](./.npmrc) | Настройки pnpm: `auto-install-peers=true`. |
| [`.prettierrc.json`](./.prettierrc.json) | Конфиг Prettier (single quotes, trailing commas). |
| [`.env.example`](./.env.example) | Пример переменных окружения (B24_APP_ID/SECRET, DATABASE_URL, порты). |
| [`README.md`](./README.md) | Инструкция по установке и запуску. |
| [`architecture.md`](./architecture.md) | Текущий документ. |
| [`docs/template-markup-guide.md`](./docs/template-markup-guide.md) | Руководство администратора по разметке `.docx`-шаблонов плейсхолдерами (`{tagKey}`/`{fieldKey}`/`{#products}`/`{%image}`), привязке формул и полей в редакторе и пошаговой миграции 12 существующих шаблонов из папки `шаблоны для первичного обращения`. |

## Пакеты

### packages/shared

Общие TypeScript-типы. Импортируется фронтендом и бэкендом через
workspace-зависимость `@b24-doc-gen/shared`. Не содержит runtime-кода —
только типы.

| Файл | Назначение |
|---|---|
| [`packages/shared/package.json`](./packages/shared/package.json) | Манифест `@b24-doc-gen/shared`. `main`/`types` указывают на `src/index.ts` (ESM re-export). |
| [`packages/shared/tsconfig.json`](./packages/shared/tsconfig.json) | TS-конфиг с `composite: true` (для project references). |
| [`packages/shared/src/index.ts`](./packages/shared/src/index.ts) | Точка входа, реэкспортирует всё из `types.ts`. |
| [`packages/shared/src/types.ts`](./packages/shared/src/types.ts) | Все интерфейсы: `AppSettings`, `Theme`, `Template` (с `formulas[]` и `fields[]`), `Formula`, `FormulaDependencies` (с опциональным `products?: boolean`), `TemplateFieldType` (`'text' \| 'textarea' \| 'number' \| 'date'`), `TemplateField` (ручное поле: `fieldKey`, `label`, `type`, `required`, `placeholder?`, `defaultValue?`, `order`), `DealField`, `EntityValues`, `ProductRow` (ID, PRODUCT_ID, PRODUCT_NAME, PRICE, QUANTITY, DISCOUNT_SUM, TAX_RATE, SUM, MEASURE_NAME, SORT, IMAGE_BASE64?, IMAGE_URL?), `FormulaContext` (DEAL, CONTACT, COMPANY, PRODUCTS: ProductRow[]), `FormulaEvaluationResult`, `B24AuthPayload`, `InstallRequest/Response`, `TemplatePreviewRequest` (`{ dealId: number; fieldValues?: Record<string,string> }` — тело POST preview), `TemplatePreviewResponse` (Фаза 2: `docxBase64: string` подставленный `.docx` в base64 + `tags: string[]` плейсхолдеры из `scanDocxPlaceholders` + `formulas: Record<string,FormulaEvaluationResult>` + `fields: TemplateField[]`; прежнее поле `html` удалено), `GenerateRequest` (с `fieldValues?: Record<string,string>`)/`GenerateResponse`, `WebhookSummary` (JSON-сериализованный снимок `Webhook` с полем `url = ${PUBLIC_URL}/api/webhook/run/${token}`, литеральный union `scope: 'theme' \| 'template'`, даты как ISO-строки), `ApiError` (legacy flat shape), `ApiErrorBody`/`ApiErrorEnvelope` (Фаза 6 — wrapped envelope от центрального error handler), `AppRole`. |

### apps/frontend

React 18 SPA, встраивается в iframe Bitrix24. Собирается Vite.

| Файл | Назначение |
|---|---|
| [`apps/frontend/package.json`](./apps/frontend/package.json) | Манифест. Зависимости: `react`, `react-dom`, `react-router-dom`, `zustand`, `@tanstack/react-query`, `@radix-ui/*`, `lucide-react`, `class-variance-authority`, `clsx`, `tailwind-merge`, `tailwindcss-animate`, `react-dropzone`, `docx-preview` (рендер подставленного `.docx` в браузере на `GeneratePage` через `renderAsync`; зависит от `jszip`), `@eigenpal/docx-editor-react` (Apache-2.0, браузерный WYSIWYG-редактор `.docx` на ProseMirror — встроен в левую панель `TemplateEditorPage` как `<DocxEditor>`; peer-зависимости `prosemirror-*` (9 пакетов) и `react-dom` ставятся автоматически через `.npmrc auto-install-peers=true`; стили подключаются из `@eigenpal/docx-editor-react/styles.css`), `@b24-doc-gen/shared` (workspace:*). |
| [`apps/frontend/tsconfig.json`](./apps/frontend/tsconfig.json) | TS-конфиг (JSX react-jsx, path alias `@/*`, reference на `packages/shared`). |
| [`apps/frontend/vite.config.ts`](./apps/frontend/vite.config.ts) | Vite-конфиг: порт 5173, proxy `/api` → `http://localhost:3001`, alias `@` → `src`. |
| [`apps/frontend/tailwind.config.ts`](./apps/frontend/tailwind.config.ts) | Tailwind-конфиг с темой shadcn/ui (HSL-переменные, анимации). |
| [`apps/frontend/postcss.config.cjs`](./apps/frontend/postcss.config.cjs) | PostCSS-конфиг (tailwindcss + autoprefixer). |
| [`apps/frontend/components.json`](./apps/frontend/components.json) | Конфиг shadcn/ui CLI. |
| [`apps/frontend/index.html`](./apps/frontend/index.html) | HTML-шаблон для Vite. |

#### apps/frontend/src

| Файл | Назначение |
|---|---|
| [`apps/frontend/src/main.tsx`](./apps/frontend/src/main.tsx) | Точка входа. Создаёт QueryClient, оборачивает `<App>` в `BrowserRouter` + `QueryClientProvider`. |
| [`apps/frontend/src/App.tsx`](./apps/frontend/src/App.tsx) | Корневой компонент с маршрутами: `/install`, `/templates`, `/templates/:id/edit`, `/generate`, `/settings`. Оборачивает дерево в `<PlacementGuard>` и монтирует глобальный `<Toaster>` (Фаза 6). |
| [`apps/frontend/src/index.css`](./apps/frontend/src/index.css) | Директивы Tailwind + CSS-переменные shadcn/ui (light/dark). |
| [`apps/frontend/src/vite-env.d.ts`](./apps/frontend/src/vite-env.d.ts) | Декларации Vite client-типов. |
| [`apps/frontend/src/lib/utils.ts`](./apps/frontend/src/lib/utils.ts) | Утилита `cn(...)` — объединение классов через `clsx` + `tailwind-merge`. |
| [`apps/frontend/src/lib/b24.ts`](./apps/frontend/src/lib/b24.ts) | Bootstrap Bitrix24 SDK. Экспортирует `initB24`, `getB24`, `getB24Auth`, `refreshB24Auth`, `getCurrentUserId`, `getCurrentDealId`, `getCurrentPlacement`, `getPlacementOptions`, `getB24AuthHeaders`, `isB24Available`, `getB24InitError`. Кеширует `B24Frame` в module-scope, поддерживает запуск вне iframe. |
| [`apps/frontend/src/lib/api.ts`](./apps/frontend/src/lib/api.ts) | Fetch-клиент бэкенда. `apiRequest<T>()` добавляет `X-B24-*` заголовки из SDK. `uploadRequest<T>(path, formData, opts)` — XHR-upload с прогрессом для multipart запросов; HTTP-метод берётся из `opts.method` (`'POST' | 'PUT'`, по умолчанию `POST`), `UploadOptions` расширен полем `method` чтобы один хелпер обслуживал и `POST /templates/upload`, и `PUT /templates/:id/docx`. Группы: `installApi` (`status`, `install`, `syncOAuth`, `registerPlacements`), `placementsApi` (`list` — GET /placements, `catalog` — GET /placements/catalog каталог мест встройки в сделку, `bind` — POST /placements встроить/перевстроить выбранное место, `unbind` — DELETE /placements; DTO `PlacementDTO`, `PlacementCatalogItemDTO`), `meApi` (`get` — GET /api/me, Фаза 6), `usersApi` (`search`), `dealApi` (`fields`, `data`), `crmApi` (`allFields` — объединённый GET /api/crm/fields для трёх сущностей, `productFields` — GET /api/crm/product-fields статический список полей товарных позиций), `themesApi` (`list`/`create`/`update`/`delete`), `templatesApi` (`list`/`get`/`preview`/`create`/`update`/`delete`/`upload`/`saveDocx`; `get(id, { withDocx? })` — GET /templates/:id, при `withDocx` возвращает `originalDocxBase64` + `docxPlaceholders` (теги из `scanDocxPlaceholders`) для редактора тегов; `preview(id, dealId, fieldValues?, signal?)` — POST /templates/:id/preview с телом `{ dealId, fieldValues }` и поддержкой `AbortSignal` для debounce-отмены, возвращает `TemplatePreviewResponseDTO`; `saveDocx(id, docx: Blob, opts?)` — PUT /templates/:id/docx через `uploadRequest` с `method: 'PUT'` (multipart `file='template.docx'`), заменяет оригинальный `.docx` отредактированным из браузерного редактора и возвращает `{ template, docxPlaceholders, warnings }` с пересканированными тегами), `formulasApi` (`validate`, `evaluate`), `generateApi` (`generate` POST /api/generate), `settingsApi` (`get`, `update`, `dealFields`, `createField`), `webhooksApi` (`list`/`create`/`patch`/`remove` — admin-only CRUD для webhook-триггеров `/api/webhooks`, типизация через `WebhookSummary` из `@b24-doc-gen/shared`; `list()` возвращает `WebhookListItemDTO[]` с дополнительными join-полями `themeName`/`templateName`). DTO: `TemplatePreviewRequestDTO` (`{ dealId, fieldValues? }` — тело POST preview), `TemplatePreviewResponseDTO` (Фаза 3: `{ docxBase64, tags, formulas, fields }`; прежнее поле `html` удалено), `GenerateResponseDTO`, `SettingsDTO`, `DealFileFieldDTO`, `FormulaEvaluationResultDTO`, `MeDTO`, `AppRoleDTO`, `WebhookListItemDTO`, `CreateWebhookBody`, `UpdateWebhookBody`. Класс `ApiError` (с полем `code`); функции `extractApiMessage(payload, fallback)` и `extractApiCode(payload)` нормализуют как новый envelope `{error:{code,message,details}}` (Фаза 6 централизованный handler), так и legacy flat shape от fastify-sensible. |
| [`apps/frontend/src/lib/formulas.ts`](./apps/frontend/src/lib/formulas.ts) | Клиентские хелперы для формул (Фаза 4). `validateLocally(expression)` — синхронная проверка пустоты/длины/сбалансированности скобок и строковых литералов. `validateRemote(expression)` — обёртка над `POST /api/formulas/validate`, нормализующая ответ в `LocalValidationResult` (valid, error?, dependencies?). `generateTagKey(label, existing)` — slugify с транслитерацией кириллицы и разрешением коллизий через суффиксы `_2`, `_3`. |
| [`apps/frontend/src/lib/formulaHelp.ts`](./apps/frontend/src/lib/formulaHelp.ts) | Справочник метаданных функций и операторов формул. Экспортирует `HELPER_DOCS` (Record по имени функции: `signature`, `summary`, `description`, `args[]`, `examples[]`) для `if`/`concat`/`format`/`dateFormat`/`upper`/`lower`/`productSum`/`productCount`/`productGet`/`productImage` и `OPERATOR_DOCS` (символ → краткое описание). Функция `extractUsedHelpers(expression)` парсит регуляркой имена функций перед `(` и возвращает `HelperDoc[]` для совпавших — используется в `EditorFormulaTooltip` для авто-показа справки по функциям, реально встретившимся в выражении. Один источник правды для `FormulaBuilder` (тултипы кнопок) и `TemplateEditorPage` (hover-подсказки над пилюлями). |
| [`apps/frontend/src/lib/templateTags.ts`](./apps/frontend/src/lib/templateTags.ts) | Хелперы панели «Теги шаблона» в `TemplateEditorPage` (Фаза 4). `RESERVED_PRODUCT_TAGS` — множество зарезервированных тегов товарного цикла (`products`, `INDEX`, `PRODUCT_NAME`, `PRICE`, `QUANTITY`, `DISCOUNT_SUM`, `TAX_RATE`, `SUM`, `MEASURE_NAME`, `SORT`, `PRODUCT_ID`, `ID`, `PREVIEW_PICTURE_BASE64`, `DETAIL_PICTURE_BASE64`, `MORE_PHOTO_BASE64`), синхронизировано с `docxTemplateEngine.buildTemplateData`. `isReservedTag(tag)` — проверка резерва. `computeTagStatus(tag, formulaKeys, fieldKeys)` → `'formula' | 'field' | 'reserved' | 'unbound'`. Тип `TagBindingStatus`. |
| [`apps/frontend/src/lib/useCurrentRole.ts`](./apps/frontend/src/lib/useCurrentRole.ts) | Хук `useCurrentRole()` (Фаза 6). TanStack Query вокруг `meApi.get()` (`GET /api/me`), `staleTime: Infinity`, enabled только если `isB24Available()`. Возвращает `{data, userId, role, isAdmin, isLoading, isError, error, refetch}`. Также экспортирует `useInvalidateRole()` (хук, возвращающий функцию `invalidateQueries(['me'])`) и константу `ME_QUERY_KEY`. |
| [`apps/frontend/src/lib/useApiError.ts`](./apps/frontend/src/lib/useApiError.ts) | Хук `useApiError()` и imperative-helper `reportApiError(err, fallbackTitle)` (Фаза 6). Распознаёт `ApiError`, обычный `Error`, строки. Подбирает русскоязычный заголовок по статусу (401/403/404/409/413/429/5xx/0). Вызывает `toast({variant: 'destructive'})` из `components/ui/use-toast.ts`. Возвращает `{status, message, code}`. |

**Pages** (заглушки, реализуются в Фазах 2–5):

| Файл | Назначение |
|---|---|
| [`apps/frontend/src/pages/InstallPage.tsx`](./apps/frontend/src/pages/InstallPage.tsx) | Первая установка. Дебаунс-поиск пользователей через `usersApi.search`, мульти-выбор админов в `Map<number, PortalUserDTO>`, сохранение через `installApi.install` + автоматический `installApi.registerPlacements`, редирект на `/templates`. |
| [`apps/frontend/src/pages/TemplatesPage.tsx`](./apps/frontend/src/pages/TemplatesPage.tsx) | Главная страница админа. Слева `<ThemeSidebar>`, справа: заголовок темы, дебаунс-поиск (`useDebouncedValue`) по имени шаблона, кнопка «Загрузить шаблон» в `<Dialog>` с `<TemplateUploader>` (обёрнута в `<AdminOnly>`), сетка карточек шаблонов выбранной темы. Каждая карточка (при `isAdmin`) содержит кнопки «Webhook» (открывает `<WebhookManagerDialog scope="template" targetId={tpl.id}>`) и «Открыть» (редактор). Запросы через `templatesApi.list({themeId, search})`. |
| [`apps/frontend/src/pages/TemplateEditorPage.tsx`](./apps/frontend/src/pages/TemplateEditorPage.tsx) | Редактор шаблона по `/templates/:id/edit` (модель тегов `.docx`, без TipTap WYSIWYG). Загружает шаблон через `templatesApi.get(id, { withDocx: true })` (возвращает `originalDocxBase64` + `docxPlaceholders`). Слева — РЕДАКТИРУЕМЫЙ браузерный редактор `.docx` через `<DocxEditor>` из `@eigenpal/docx-editor-react` (Фаза 2 заменила прежний read-only `docx-preview`): `documentBuffer` выводится `useMemo` из `originalDocxBase64` → `base64ToBytes(b64).buffer` (cast в `ArrayBuffer`), props `ref={editorRef}` (`useRef<DocxEditorRef>`), `mode='editing'`, `showToolbar`, `showRuler`, `documentName={name}`, `onChange` → `setDocDirty(true)`, `onError` → `setEditorError`; редактор обёрнут в контейнер-изолятор (класс `docx-editor-host` + `isolate [contain:layout_paint]`) чтобы Word-стили `styles.css` не протекали в остальной UI; при отсутствии `.docx` показывается плейсхолдер, при незагруженном буфере — спиннер. Справа — панель «Теги шаблона»: для каждого плейсхолдера из `scanDocxPlaceholders` показывает статус через `computeTagStatus` (`lib/templateTags.ts`): formula / field / reserved (продуктовые теги цикла) / unbound (подсветка amber). Действия на тег: привязать формулу (`<FormulaBuilder>` + `<FieldPicker>`, `tagKey` = имя тега) или объявить ручным полем (`<ManualFieldBuilder>`, `fieldKey` = имя тега), изменить или удалить привязку. Один тег = одна привязка (взаимоисключающе). Над редактором — кнопки «Вставить формулу»/«Вставить поле» (insert-режим, аналог прежней кнопки Σ TipTap): `openInsertFormula`/`openInsertField` открывают те же билдеры с чистой формой (`insertMode=true`, `activeTag=null`), а `handleConfirmFormula`/`handleConfirmField` в этой ветке создают привязку по `result.tagKey`/`result.fieldKey` и вставляют `{tagKey}` в позицию курсора через ProseMirror (`editorRef.current.getEditorRef().getView()` → `view.dispatch(state.tr.insertText(…))`, хелпер `insertPlaceholderAtCursor`), помечают `docDirty` и добавляют тег в `locallyInsertedTags` (теги, вставленные в сессии до сохранения; мерджатся в список `tags` для немедленного отображения в панели, сбрасываются при загрузке шаблона). Привязки в локальных картах `formulasByKey`/`fieldsByKey` по имени тега; `activeTag` хранит цель открытого диалога (bind-режим). Сохранение `handleSave` (асинхронный последовательный поток, одна кнопка): (1) если `docDirty` — `await editorRef.current.save()` (`Promise<ArrayBuffer|null>`, null-guard) → `new Blob([buf], {docx mime})` → `templatesApi.saveDocx(id, blob)` (заменяет `originalDocx`, пересканирует теги), затем `queryClient.setQueryData(['template',id,'withDocx'], …)` мерджит новые `docxPlaceholders` (сохраняя base64) + `invalidateQueries` и `setDocDirty(false)`; (2) `window.confirm`-предупреждение при несвязанных тегах — ПОСЛЕ пересканирования (набор тегов мог измениться); (3) `PUT /api/templates/:id` через `saveMutation` с массивами `formulas[]`/`fields[]`. При ошибке шага 1 шаги 2–3 не выполняются. Состояние: `docDirty`, `editorError`, `isSavingDocx`; `isSaving = isSavingDocx || saveMutation.isPending` управляет спиннером кнопки; кнопка активна при `dirty || docDirty`. `saveMutation.onSuccess` мерджит `withDocx:false` DTO в кэш, сохраняя `originalDocxBase64`/`docxPlaceholders`/`hasOriginalDocx`. Поля имени/темы и кнопка Сохранить — в шапке. Орфанные привязки (ключ без тега в `.docx`) показываются отдельной секцией для удаления. Вложенные компоненты `TagRow`, `StatusBadge`. `docx-preview`/`renderAsync` здесь больше НЕ используются (остаются только на `GeneratePage`). |
| [`apps/frontend/src/pages/GeneratePage.tsx`](./apps/frontend/src/pages/GeneratePage.tsx) | Preview + генерация документа (Фаза 3: preview через `docx-preview`). Три колонки: слева — сайдбар тем+шаблонов (`themesApi.list` → развёртывание выбранной темы с `templatesApi.list({themeId})`), центр — preview подставленного `.docx`: `templatesApi.preview(id, dealId, fieldValues, signal)` (POST) возвращает `{ docxBase64, tags, formulas, fields }`, base64 декодируется в `Uint8Array` (`base64ToBytes`) и рендерится клиентом через `docx-preview` `renderAsync(bytes, bodyContainer, styleContainer, opts)` в реф `previewBodyRef`; опции `className='gen-docx-preview'`, `inWrapper`, `ignoreWidth/Height=false` (1:1 с Word), `breakPages`, `useBase64URL` (картинки inline), `renderHeaders/Footers/Footnotes`; стили Word рендерятся в отдельный скрытый `previewStyleRef` чтобы не протекать в UI; контейнер очищается (`replaceChildren`) перед каждым рендером, флаг `cancelled` защищает от out-of-order рендеров при быстром вводе. Справа — sidebar действий с формой ручных полей «Поля для заполнения» (`previewData.fields` через вложенный `ManualFieldInput` по типу text/textarea/number/date), кнопкой «Сгенерировать документ» (мутация `generateApi.generate` с `fieldValues`), результат (fileName, downloadUrl, binding, timeline, warnings[]), список формул шаблона. State `fieldValues: Record<fieldKey,string>` (сброс при смене темы/шаблона; при загрузке `previewData` поля с `defaultValue` предзаполняются через `initialFieldValue` — `date`/`today` → текущая дата (`todayISO`), text/textarea/number → литеральное значение). Live-обновление: `fieldValues` дебаунсятся 500мс (`useDebouncedValue`) и входят в `queryKey` preview-запроса, поэтому пауза в наборе перезапрашивает preview; React Query передаёт `AbortSignal` в `queryFn`, отменяя устаревшие запросы; `placeholderData` сохраняет предыдущий кадр во время дозагрузки. `formatFieldValue` конвертирует `date` ISO→`dd.MM.yyyy`. Кнопка генерации блокируется при незаполненных обязательных полях (`missingRequired`), ошибке preview или загрузке. Когда у шаблона нет `originalDocx` (backend → HTTP 400), `previewErrorMessage` показывает понятное сообщение вместо краша; ошибки самого `renderAsync` отражает `renderError`. Если `getCurrentDealId()` возвращает null — рендерит stub с просьбой открыть из карточки сделки. |
| [`apps/frontend/src/pages/SettingsPage.tsx`](./apps/frontend/src/pages/SettingsPage.tsx) | Настройки приложения (Фаза 5). Секции: 1) Поле для сгенерированных файлов — `<select>` из `settingsApi.dealFields()` (только `USER_TYPE_ID=file`), с кнопкой «Создать поле» → Dialog с inputs для XML_ID (sanitized A-Z/0-9/_) и label → `settingsApi.createField` → refetch; 2) Администраторы — список текущих с кнопкой Trash + debounced поиск через `usersApi.search` + чекбоксы добавления; 3) Места встройки — список привязанных обработчиков (`placementsApi.list`) с кнопкой Trash → confirm-Dialog → `placementsApi.unbind` (удалить привязку), плюс блок «Добавить место встройки»: `<select>` из `placementsApi.catalog()` (помечает уже встроенные через `boundPlacementCodes` Set, дефолт — первое не встроенное) и кнопка «Встроить» → `bindMutation` (`placementsApi.bind`) для встройки/повторной встройки; 4) Save-bar с `settingsApi.update({dealFieldBinding, adminUserIds})` и toast-стрипом. Использует `useDebouncedValue`, `ApiError`. |

**UI-компоненты shadcn/ui** (в `src/components/ui/`):

| Файл | Назначение |
|---|---|
| [`apps/frontend/src/components/ui/button.tsx`](./apps/frontend/src/components/ui/button.tsx) | `Button` + `buttonVariants` (CVA: variant/size). |
| [`apps/frontend/src/components/ui/input.tsx`](./apps/frontend/src/components/ui/input.tsx) | `Input` — обёртка над native `<input>`. |
| [`apps/frontend/src/components/ui/dialog.tsx`](./apps/frontend/src/components/ui/dialog.tsx) | `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`, `DialogClose`, `DialogOverlay`, `DialogPortal` (Radix). |
| [`apps/frontend/src/components/ui/dropdown-menu.tsx`](./apps/frontend/src/components/ui/dropdown-menu.tsx) | `DropdownMenu` + `Trigger`, `Content`, `Item`, `CheckboxItem`, `RadioItem`, `Label`, `Separator`, `Sub*`, `Group` (Radix). |
| [`apps/frontend/src/components/ui/toast.tsx`](./apps/frontend/src/components/ui/toast.tsx) | `Toast`, `ToastProvider`, `ToastViewport`, `ToastAction`, `ToastClose`, `ToastTitle`, `ToastDescription` (Radix). |
| [`apps/frontend/src/components/ui/use-toast.ts`](./apps/frontend/src/components/ui/use-toast.ts) | Хук `useToast()` и imperative-функции `toast({title, description, variant, duration, action})`/`dismiss(id)` (Фаза 6). Простой in-memory store со списком слушателей и лимитом `TOAST_LIMIT=5`. Авто-удаление через `TOAST_REMOVE_DELAY=5s`. |
| [`apps/frontend/src/components/ui/toaster.tsx`](./apps/frontend/src/components/ui/toaster.tsx) | Хост-компонент `<Toaster>` (Фаза 6). Подписывается на `useToast()` и рендерит каждый toast внутри `<ToastProvider>` + `<ToastViewport swipeDirection="right">`. Монтируется один раз в `App.tsx`. |
| [`apps/frontend/src/components/ui/tabs.tsx`](./apps/frontend/src/components/ui/tabs.tsx) | `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` (Radix). |
| [`apps/frontend/src/components/ui/RichTooltip.tsx`](./apps/frontend/src/components/ui/RichTooltip.tsx) | Лёгкий тултип в стиле Google Sheets без зависимости от Radix. Оборачивает `children` в inline-flex span, на hover/focus с задержкой 250 мс показывает богатое содержимое (`ReactNode`) через `createPortal` в `document.body`. `useLayoutEffect` пересчитывает позицию: центрирование по горизонтали, клэмп по краям viewport, флип сверху↔снизу. Props: `content`, `children`, `delay`, `side`, `className`, `asBlock`. |

**Прочие компоненты** (в `src/components/`):

| Файл | Назначение |
|---|---|
| [`apps/frontend/src/components/PlacementGuard.tsx`](./apps/frontend/src/components/PlacementGuard.tsx) | Обёртка над всем route-tree. Проверяет `isB24Available()`, `GET /api/install/status`, жёсткое правило: в контексте карточки сделки (`isDealCardContext()` — placement начинается с `CRM_DEAL_DETAIL` ИЛИ есть `getCurrentDealId()`) **всегда** форсит `/generate`, игнорируя `?view=`, роль и привязку (даже админ в сделке видит только генерацию; исключение — `/install`). Вне сделки роутит по `?view=` и `defaultPathForPlacement` (любой `CRM_DEAL_DETAIL*` → `/generate`; `DEFAULT` → `/templates`). При отсутствии установки редиректит на `/install`. После установки вызывает `useCurrentRole()`; если пользователь не админ и пытается открыть `/settings` (или `DEFAULT` placement, ведущий на настройки) — рендерит 403-stub вместо детей (Фаза 6). |
| [`apps/frontend/src/components/AdminOnly.tsx`](./apps/frontend/src/components/AdminOnly.tsx) | Wrapper-компонент `<AdminOnly>` (Фаза 6). Использует `useCurrentRole()` и рендерит `children` только если текущий пользователь — админ; иначе рендерит проп `fallback` (по умолчанию `null`). Во время загрузки роли тоже рендерит fallback, чтобы не было flash-а админских кнопок. |
| [`apps/frontend/src/components/ThemeSidebar.tsx`](./apps/frontend/src/components/ThemeSidebar.tsx) | Левый сайдбар тем (Фаза 3). Загружает темы через TanStack Query (`['themes']` ↔ `themesApi.list`). Кнопка «+» открывает `<Dialog>` для создания. На каждой теме `<DropdownMenu>` с пунктами Настройки темы / Webhook темы / Переименовать / Удалить. Пункт «Webhook темы» открывает `<WebhookManagerDialog scope="theme" targetId={theme.id}>`. Удаление с 409 (есть шаблоны) показывается inline-баннером. Контролируемый выбор `selectedThemeId` через проп `onSelect`. Кнопка «+» и DropdownMenu действий по теме отрисовываются только для админа (`useCurrentRole().isAdmin`, Фаза 6). |
| [`apps/frontend/src/components/WebhookManagerDialog.tsx`](./apps/frontend/src/components/WebhookManagerDialog.tsx) | Модал управления webhook-триггерами для Theme или Template (Фаза 5 вебхуков). Принимает `{scope: 'theme'\|'template', targetId: string\|null, targetName?: string\|null, onClose}`. Загружает полный список через `webhooksApi.list()` (TanStack Query `['webhooks']`) и фильтрует по `scope`+`targetId` на клиенте. Кнопка «Создать новый» → `webhooksApi.create({scope, themeId\|templateId})`. Для каждого webhook'а: readonly `<Input>` с URL, кнопка «Скопировать» (async Clipboard API с fallback на `document.execCommand('copy')` для iframe-политик Bitrix24, показывает toast через `useToast()`), чекбокс enabled (`webhooksApi.patch(id, {enabled})`), кнопка удаления с `window.confirm` и `webhooksApi.remove(id)`. Help-блок с инструкцией вставить URL в робот «Исходящий вебхук» (метод POST). Видимость — вся функциональность admin-only, так как гейтинг делается на уровне точек входа (ThemeSidebar dropdown / TemplatesPage isAdmin-блок). |
| [`apps/frontend/src/components/TemplateUploader.tsx`](./apps/frontend/src/components/TemplateUploader.tsx) | Drag&drop загрузка `.docx` (Фаза 3). Использует `react-dropzone` с фильтрами `application/vnd.openxmlformats-officedocument.wordprocessingml.document` и `maxSize: 20MB`. Прогресс через `templatesApi.upload` (XHR). Колбэки `onSuccess(template)` и `onError(message)`. Принимает `themeId` как required prop. |
| [`apps/frontend/src/components/ManualFieldBuilder.tsx`](./apps/frontend/src/components/ManualFieldBuilder.tsx) | Модалка конструктора ручного поля (shadcn Dialog). Inputs: `label`, `fieldKey` (авто-slug через `generateTagKey`, с флагом `keyDirty`), `<select>` типа (text/textarea/number/date), блок «Значение по умолчанию» (для `date` — `<select>` Не задано/Сегодня → `'today'`; для `textarea` — textarea; для `text`/`number` — input с литералом, который пользователь сможет изменить; сбрасывается при смене типа), чекбокс «Обязательное», input `placeholder`. Поддержка EDIT-режима (`initialValues`) и проверки уникальности (`existingKeys`). На submit вызывает `onInsert({fieldKey, label, type, required, placeholder, defaultValue})` (`defaultValue` обнуляется для не-date). Экспортирует `ManualFieldBuilderResult`. |
| [`apps/frontend/src/components/FieldPicker.tsx`](./apps/frontend/src/components/FieldPicker.tsx) | Дерево CRM-полей для вставки токенов в выражение. Вкладки Сделка/Контакт/Компания/Товары (`Tabs` shadcn), поиск по коду или названию, рендер списка с типом поля. Данные для Deal/Contact/Company грузит через `crmApi.allFields()` (TanStack Query, staleTime 5 минут); поля товаров (`PRODUCT_FIELDS`) захардкожены статически. На клик вызывает `onSelect(token, field, entity)` — для DEAL/CONTACT/COMPANY токен вида `DEAL.OPPORTUNITY`, для PRODUCT — `productGet(1, "FIELD")`. |
| [`apps/frontend/src/components/FormulaBuilder.tsx`](./apps/frontend/src/components/FormulaBuilder.tsx) | Модалка конструктора формул (shadcn Dialog). Сетка 2 колонки: слева — label, tagKey (авто-slug через `generateTagKey`, с флагом `tagKeyDirty`), монопространственная textarea, палитра операторов и helper-функций (if/concat/format/dateFormat/upper/lower) — каждая кнопка обёрнута в `<RichTooltip>` с богатой подсказкой в стиле Google Sheets: для функций — `HelperTooltipContent` (signature, описание, таблица аргументов, пример) из общего справочника `HELPER_DOCS` (`lib/formulaHelp.ts`), для операторов — описание из `OPERATOR_DOCS`. Отдельная палитра «Товары» с emerald-кнопками для product-хелперов (productSum/productCount/productGet/productImage). Блок предпросмотра (при наличии `testDealId` вызывает `formulasApi.evaluate`). Справа — `FieldPicker` и блок зависимостей. Live-валидация: `validateLocally` синхронно + debounce 500 мс `validateRemote`. Кнопка «Вставить» заблокирована при ошибках; на submit вызывает `onInsert({tagKey, label, expression, dependsOn})`. Экспортирует `HelperTooltipContent` для повторного использования в `EditorFormulaTooltip`. |

Подкаталоги `src/lib/`, `src/store/` дополняются по мере развития
(zustand-слайсы — Фаза 5).

### apps/backend

Fastify REST API на TypeScript. Использует Prisma для работы с SQLite.

| Файл | Назначение |
|---|---|
| [`apps/backend/package.json`](./apps/backend/package.json) | Манифест. Зависимости: `fastify`, `@fastify/cors`, `@fastify/formbody`, `@fastify/http-proxy`, `@fastify/multipart`, `@fastify/sensible`, `@fastify/static`, `qs`, `dotenv`, `@prisma/client`, `mammoth`, `mathjs`, `@turbodocx/html-to-docx`, `pizzip`, `docxtemplater`, `docxtemplater-image-module-free`, `@b24-doc-gen/shared` (workspace:*). DevDeps: `tsx`, `prisma`, `pino-pretty`, `@types/qs`. Использует глобальный `fetch` (Node 18+) и `node:crypto` для HMAC. |
| [`apps/backend/tsconfig.json`](./apps/backend/tsconfig.json) | TS-конфиг (module NodeNext, path alias `@/*`, reference на `packages/shared`). |
| [`apps/backend/.env`](./apps/backend/.env) | Локальные переменные (DATABASE_URL, BACKEND_PORT, FRONTEND_URL, NODE_ENV). Не коммитится. |

#### apps/backend/src

| Файл | Назначение |
|---|---|
| [`apps/backend/src/server.ts`](./apps/backend/src/server.ts) | Точка входа. Экспортирует `buildServer()` — фабрику Fastify-инстанса (регистрирует CORS, `@fastify/formbody` с парсером `qs.parse` для nested-bracket полей `auth[access_token]`/`document_id[2]`, multipart, sensible, B24 auth middleware, маршруты health/deal/users/install/themes/templates/formulas/settings/generate/me/webhooks/webhookRun) — и `start()` — процесс-уровневый запуск на `BACKEND_PORT`. Централизованный `setErrorHandler` конвертирует все ошибки в `{error: {code, message, details?}}`, 5xx логгируются через `request.log.error` со stack, в dev-режиме stack включается в `details`. |
| [`apps/backend/src/routes/health.ts`](./apps/backend/src/routes/health.ts) | `registerHealthRoute(app)` — регистрирует `GET /health`, возвращает `{status, service, uptime, timestamp}`. |
| [`apps/backend/src/routes/me.ts`](./apps/backend/src/routes/me.ts) | `registerMeRoutes(app)` (Фаза 6) — `GET /api/me`. Возвращает `{userId, role}` где `role: 'admin' | 'user'` определяется через `loadCurrentRole(request)` (см. `middleware/role.ts`). Используется фронтендом для решения, какие UI-элементы показывать. |
| [`apps/backend/src/routes/deal.ts`](./apps/backend/src/routes/deal.ts) | `registerDealRoutes(app)` — `GET /api/deal/:id/fields` (кеш TTL 5 минут per portal), `GET /api/crm/fields` (объединённый батч-ответ с полями для Deal/Contact/Company, кешируется тремя отдельными `TTLCache` инстансами), `GET /api/crm/product-fields` (статический список полей товарных позиций: PRODUCT_NAME, PRICE, QUANTITY, DISCOUNT_SUM, TAX_RATE, SUM, MEASURE_NAME), `GET /api/deal/:id/data` (двухступенчатый `callBatch`: deal+contacts → contact+company). Экспортирует `invalidateDealFieldsCache()`. |
| [`apps/backend/src/routes/formulas.ts`](./apps/backend/src/routes/formulas.ts) | `registerFormulaRoutes(app)` (Фаза 4) — `POST /api/formulas/validate` (использует `formulaEngine.validateExpression` + trial `evaluateExpression` с пустым контекстом `{DEAL:{}, CONTACT:{}, COMPANY:{}, PRODUCTS:[]}` для обнаружения runtime-ошибок в product-хелперах, возвращает `{valid, error?, dependencies}`) и `POST /api/formulas/evaluate` (при наличии `dealId` собирает контекст через двухступенчатый batch как в `deal.ts`, поддерживает inline `context` override, вызывает `formulaEngine.evaluateExpression`, возвращает `{ok, value, raw, error?, dependencies}`). |
| [`apps/backend/src/routes/users.ts`](./apps/backend/src/routes/users.ts) | `registerUsersRoutes(app)` — `GET /api/users?search=&start=` (нормализует `user.get` в `PortalUser[]` со стабильной формой `id/name/lastName/fullName/email/active`). |
| [`apps/backend/src/routes/install.ts`](./apps/backend/src/routes/install.ts) | `registerInstallRoutes(app)` — `GET /api/install/status`, `POST /api/install` (upsert `AppSettings` id=1, JSON-encoded `adminUserIds`, portal из `request.b24Auth`, опц. сохранение OAuth-снапшота, после upsert вызывает `invalidateRoleCache()`), `POST /api/install/sync-oauth`, `POST /api/install/register-placements` (placement.bind для `CRM_DEAL_DETAIL_TAB` при установке). Управление местами встройки для UI настроек: `GET /api/placements` (placement.get — список привязанных обработчиков), `GET /api/placements/catalog` (отдаёт `DEAL_PLACEMENT_CATALOG` — каталог поддерживаемых мест встройки в сделку code+title+description для дропдауна «куда встроить»), `POST /api/placements` (bind выбранного места: валидирует код по `DEAL_PLACEMENT_CATALOG`, строит `HANDLER=base/?view=<view>`, `placement.bind`, `ERROR_PLACEMENT_ALREADY_BOUND` трактуется как успех → `{ok, placement, alreadyBound?}`), `DELETE /api/placements` (placement.unbind по `{placement, handler}`). Экспортирует `toAppSettings(row)`. Внутренние: `resolveHandlerBaseUrl(explicit, reply)` — резолвит+валидирует HTTPS base URL обработчика (приоритет explicit > `FRONTEND_PUBLIC_URL` > `PUBLIC_URL` > `FRONTEND_URL`); const `DEAL_PLACEMENT_CATALOG` (`CRM_DEAL_DETAIL_TAB`, `CRM_DEAL_DETAIL_TOOLBAR`, `CRM_DEAL_DETAIL_ACTIVITY`, все `view=generate`). |
| [`apps/backend/src/routes/themes.ts`](./apps/backend/src/routes/themes.ts) | `registerThemeRoutes(app)` — CRUD тем шаблонов: `GET /api/themes` (сортировка по `order` ASC, затем по имени, с `_count.templates`), `POST /api/themes` (новая тема, order по умолчанию = max+1), `PUT /api/themes/:id` (обновление name/order), `DELETE /api/themes/:id` (возвращает 409, если есть привязанные шаблоны). Экспортирует `ThemeDTO`. Mutation-роуты POST/PUT/DELETE гейтятся `requireAdmin` (Фаза 6, см. `middleware/role.ts`). |
| [`apps/backend/src/routes/templates.ts`](./apps/backend/src/routes/templates.ts) | `registerTemplateRoutes(app)` — CRUD шаблонов и multipart-загрузка `.docx`: `GET /api/templates?themeId=&search=` (фильтр по теме + поиск contains по имени, с `themeName` и `_count.formulas`), `GET /api/templates/:id?withDocx=1` (шаблон + массивы формул и ручных полей; при `withDocx` — base64 оригинала и `docxPlaceholders` (хелпер `toTemplateDto` сканирует `.docx` через `scanDocxPlaceholders` — теги для панели редактора, без контекста сделки)), `POST /api/templates/:id/preview` (Фаза 2: тело `TemplatePreviewRequest` `{ dealId, fieldValues? }`; загружает шаблон + формулы + поля, гард — нет `originalDocx` → `reply.badRequest`, иначе приводит `originalDocx` к Buffer; detect product usage (как в generate); через `getDealContext` собирает контекст сделки (`DealDataError` → 404/400/badGateway); вычисляет каждую формулу через `formulaEngine.evaluateExpression` → `formulaResults[tagKey]`; разрешает ручные поля через `resolveManualFieldValues(row.fields, fieldValues)` (переиспользован из `generationPipeline.ts`); подставляет формулы + продукты + поля прямо в оригинальный `.docx` через `docxTemplateEngine.buildDocxFromTemplate(originalDocx, {formulas, products, fieldValues, title})` (`DocxTemplateError` → `reply.badRequest`); собирает плейсхолдеры через `scanDocxPlaceholders(originalDocx)`; возвращает `TemplatePreviewResponse` с `docxBase64` (base64 подставленного `.docx`), `tags[]`, `formulas[tagKey]` и `fields[]` (сортировка по `order`)), `POST /api/templates` (создать пустой), `POST /api/templates/upload` (multipart, max 20MB, валидация .docx-расширения и mime, парсинг через `docxParser`, сохранение `originalDocx` в Bytes), `PUT /api/templates/:id/docx` (multipart-замена оригинального `.docx` отредактированным из браузерного редактора: зеркалит `POST /api/templates/upload` — `requireAdmin`, гард `isMultipart`, `request.file` лимит 20MB, валидация `.docx` по расширению/mime, `toBuffer` + проверка `truncated`; отличия — сначала `prisma.template.findUnique` (нет → `reply.notFound`), затем `prisma.template.update` `originalDocx`+пересчитанный `contentHtml`; `contentHtml` пересчитывается через `parseDocxToHtml` (legacy/поиск, `DocxParseError` → `badRequest`), теги — через `scanDocxPlaceholders(buffer)` (non-fatal, при ошибке warning); ответ `{ template, warnings, docxPlaceholders }` с кодом 200 — это обновление, не создание), `PUT /api/templates/:id` (транзакционное обновление name/themeId/contentHtml + замена массивов `formulas` и `fields` через deleteMany+createMany), `DELETE /api/templates/:id` (удаляет formulas + fields + template). Хелперы `toTemplateFieldDto`, `normalizeFieldsInput` (валидация типа по белому списку, dedup по `fieldKey`, `order` по позиции, `defaultValue` для `date` — только токен из `FIELD_DEFAULTS`={`today`}, для остальных типов — литерал (trim, ≤2000 символов)). Экспортирует `TemplateListItemDTO`, `TemplateDTO` (с `fields`), `FormulaInput`, `TemplateFieldInput`. Mutation-роуты POST `/api/templates`, POST `/api/templates/upload`, PUT `/api/templates/:id/docx`, PUT `/api/templates/:id`, DELETE `/api/templates/:id` гейтятся `requireAdmin` (Фаза 6). |
| [`apps/backend/src/routes/settings.ts`](./apps/backend/src/routes/settings.ts) | `registerSettingsRoutes(app)` (Фаза 5) — `GET /api/settings` (текущая `AppSettings`), `PUT /api/settings` (обновление `dealFieldBinding` и/или `adminUserIds`, валидирует `UF_CRM_*` формат, после смены `adminUserIds` зовёт `invalidateRoleCache()`), `GET /api/settings/deal-fields` (через `b24Client.listDealUserFields`, фильтр по `USER_TYPE_ID = file`, нормализация в `DealFileFieldDTO[]` с локализованными метками через `pickLocalized`), `POST /api/settings/create-field` (создаёт `UF_CRM_*` поле через `b24Client.addDealUserField` с `USER_TYPE_ID: file`, обработка дублей XML_ID как 409). Использует `toAppSettings()` из `install.ts`. Mutation-роуты PUT `/api/settings` и POST `/api/settings/create-field` гейтятся `requireAdmin` (Фаза 6). |
| [`apps/backend/src/routes/webhooks.ts`](./apps/backend/src/routes/webhooks.ts) | `registerWebhookRoutes(app)` — admin-only CRUD для webhook-триггеров Bitrix24-робота «Исходящий вебхук». `GET /api/webhooks` (список с join'ом `theme`/`template` по имени, сортировка по `createdAt desc`), `POST /api/webhooks` (body `{scope, themeId?, templateId?, label?}`, генерирует `token = randomBytes(24).toString('base64url')`, валидирует существование темы/шаблона, retry x3 на `P2002` коллизии), `PATCH /api/webhooks/:id` (только `label` и `enabled` — прочие поля иммутабельны), `DELETE /api/webhooks/:id` (204). Все роуты гейтятся `requireAdmin` из `middleware/role.ts`. Внутренний хелпер `resolvePublicBaseUrl()` читает `PUBLIC_URL`/`FRONTEND_PUBLIC_URL`/`FRONTEND_URL` (аналогично `routes/install.ts`). DTO — `WebhookSummary` из `@b24-doc-gen/shared` с `url = ${publicUrl}/api/webhook/run/${token}`; в GET-ответе дополнительно присутствуют поля `themeName`/`templateName` для UI. |
| [`apps/backend/src/routes/generate.ts`](./apps/backend/src/routes/generate.ts) | `registerGenerateRoutes(app)` — `POST /api/generate { templateId, dealId, fieldValues? }`. Тонкий HTTP-адаптер: аутентификация через `request.b24Auth`, валидация `templateId`/`dealId`, затем строгая предварительная проверка обязательных ручных полей (загружает только `fields` через `select`, `resolveManualFieldValues(fields, body.fieldValues)`; при пустом обязательном поле → `reply.badRequest` со списком меток — в отличие от пайплайна, который лишь предупреждает). После валидации делегирует ВЕСЬ конвейер в `runGeneration({templateId, dealId, client, logger, fieldValues})` из `generationPipeline.ts` (без дублирующего inline-пути сборки). Маппинг `GenerationError.kind` → Fastify-ответы: `template_not_found`/`deal_not_found` → 404, `bad_deal_id`/`docx_build_failed` → 400, `deal_gateway`/`disk_gateway`/`upload_failed` → 502. Итоговый артефакт — `.docx` (рендерится `buildDocxFromTemplate` внутри `runGeneration`). Возвращает `GenerateRouteResponse {fileId, fileName, downloadUrl, formulas, binding, timeline, warnings[]}` (= форма `GenerationResult`). |
| [`apps/backend/src/routes/webhookRun.ts`](./apps/backend/src/routes/webhookRun.ts) | `registerWebhookRunRoute(app)` (Фаза 3 вебхуков) — публичный `POST /api/webhook/run/:token`, принимает `application/x-www-form-urlencoded` от Bitrix24-робота «Исходящий вебхук». Шаги: 1) `prisma.webhook.findUnique({token})` → 404 если нет/`!enabled`, 2) валидация `auth.application_token` против `AppSettings.applicationToken` → 401 на mismatch, 3) `parseDealIdFromDocumentId(body.document_id[2])` — регулярки `/^DEAL_(\d+)$/i` и `/^DEAL_(?:FLEXIBLE|FLEXABLE)_(\d+)_(\d+)$/i` → 400 при неизвестном формате, 4) `new B24Client({portal: auth.domain, accessToken: auth.access_token})`, 5) resolve templateIds из scope (`template` → один id, `theme` → `prisma.template.findMany({themeId})`), 6) последовательный `for..of` с `runGeneration()` — каждый шаблон в try/catch (`GenerationError`/`B24Error`/unknown) с записью в `results[]`, счётчики `generated`/`failed`, 7) best-effort `prisma.webhook.update` (useCount++ / lastUsedAt=now), 8) опциональный ack через `bizproc.event.send` если присутствует `body.event_token` (ошибки ack'а игнорируются), 9) ответ `{ok, dealId, generated, failed, results[]}`. Внутренние хелперы: `readDocumentIdEntry()` (нормализует qs-массив и qs-объект), `parseDealIdFromDocumentId()` (экспортируется для тестов), `pickString()`, `summariseSuccess()`, `sendBizprocAck()`. |
| [`apps/backend/src/services/generationPipeline.ts`](./apps/backend/src/services/generationPipeline.ts) | Framework-agnostic ядро генерации, вынесенное из `routes/generate.ts` для переиспользования в UI-роуте, bizproc-хендлере и webhook-раннере. Экспортирует `runGeneration({templateId, dealId, client, logger, fieldValues?}): Promise<GenerationResult>` — шаги: загрузка template+formulas+fields+AppSettings → `detectProductUsage` (сканирование HTML и формул на product-хелперы) → deal context с `fetchProducts`/`fetchProductImages` → evaluate formulas → нормализация значений ручных полей через `resolveManualFieldValues` (экспортируется отсюда; `fieldValues` опциональны: server-to-server вызовы без них → подставляются дефолты типа `date`/`today`, иначе пусто; для незаполненных обязательных добавляется `warning`, без падения) → сборка `.docx` через `buildDocxFromTemplate(template.originalDocx, {formulas, products: context.PRODUCTS, fieldValues, title})` напрямую в оригинальный Word-архив, без HTML→PDF шага (гард: отсутствует/пустой `originalDocx` → `GenerationError('docx_build_failed')`; `Bytes` приводится к `Buffer`; `DocxTemplateError` маппится в `GenerationError('docx_build_failed')`) → `disk.storage.getforapp` → `uploadDiskFile` (имя файла `<name>_deal<id>_<ts>.docx`) → опциональный `crm.item.update` по `UF_CRM_*` → опциональный `crm.timeline.comment.add` → возврат `{fileId, downloadUrl, fileName, formulas, binding, timeline, warnings[]}`. Приватный хелпер `detectProductUsage(contentHtml, expressions)` определяет нужна ли загрузка товаров/картинок. Детерминированные ошибки пробрасываются как `GenerationError` с `kind`. Экспортирует типы `GenerationResult`, `BindingResult`, `GenerationErrorKind`, `RunGenerationParams` (с `fieldValues?`), класс `GenerationError` и функцию `resolveManualFieldValues(fields, rawValues)` (разрешение значений ручных полей + дефолтов, переиспользуется в `routes/generate.ts`). |
| [`apps/backend/src/middleware/auth.ts`](./apps/backend/src/middleware/auth.ts) | `registerAuthMiddleware(app)` — preHandler hook на `/api/*` (кроме путей из `PUBLIC_PATHS = ['/health', '/api/health', '/api/webhook/run']` — `isPublicPath` матчит exact и `prefix/` варианты, поэтому `/api/webhook/run/<token>` публичный), извлекает `X-B24-Access-Token`/`Member-Id`/`Domain` (или body.auth), валидирует через `verifyB24Payload`, опционально проверяет HMAC-подпись (`B24_APP_SECRET`), заполняет `request.b24Auth` `{userId, domain, accessToken, memberId}`. `userId` берётся из `x-b24-user-id`/body при наличии, иначе резолвится `resolveUserIdFromB24(domain, accessToken)` через REST `user.current`. ВАЖНО: кэш `userId` (TTL 5 мин) ключуется по **access-токену** (per-user), а не по `memberId` (он общий для всего портала — иначе все пользователи получили бы `userId` того, кто открыл приложение первым, и роль админа). Экспортирует `B24RequestAuth`, `verifyB24Payload`, `isPlausibleBitrixDomain`. Расширяет тип `FastifyRequest`. |
| [`apps/backend/src/middleware/role.ts`](./apps/backend/src/middleware/role.ts) | Role-based access control (Фаза 6). `requireAdmin(request, reply)` — Fastify preHandler, отдаёт 403 если `request.b24Auth.userId` отсутствует в `AppSettings.adminUserIds`. `loadCurrentRole(request)` — резолвит роль не бросая исключений (используется в `routes/me.ts`). Внутри — in-memory cache набора admin id (`Set<number>`) с TTL 30 секунд. `invalidateRoleCache()` сбрасывает кеш — вызывается из `routes/install.ts` (после upsert) и `routes/settings.ts` (после смены adminUserIds). |
| [`apps/backend/src/services/b24Client.ts`](./apps/backend/src/services/b24Client.ts) | Класс `B24Client({portal, accessToken, fetchImpl?, timeoutMs?})`. Методы: `callMethod`, `callBatch`, `getDeal`, `getDealFields` (нормализует в `DealField[]`), `getContactFields`, `getCompanyFields`, `listDealUserFields`, `addDealUserField`, `updateDeal`, `getContact`, `getCompany`, `getDealContacts`, `addTimelineComment`, `listUsers`, `uploadDiskFile`, `getDealProductRows(dealId)` (вызывает `crm.deal.productrows.get`, нормализует в `ProductRow[]`), `getProductImages(productId)` (вызывает `catalog.productImage.list`, возвращает `{downloadUrl, type}[]`, ошибки scope проглатываются), `downloadFileAsBase64(url)` (скачивает файл по URL с `?auth=accessToken`, возвращает `data:mime;base64,...` строку). Класс ошибок `B24Error(code, status, details)`. Приватный хелпер `normalizeProductRow` для приведения raw-записей к `ProductRow`. Сериализует параметры батча в PHP-стиль (`fields[NAME]=foo`). |
| [`apps/backend/src/services/cache.ts`](./apps/backend/src/services/cache.ts) | `TTLCache<V>` — простой in-memory кеш с per-key TTL. Методы: `get`, `set`, `delete`, `clear`, `size`. |
| [`apps/backend/src/services/docxParser.ts`](./apps/backend/src/services/docxParser.ts) | **@deprecated (legacy-модель).** `parseDocxToHtml(buffer: Buffer)` — конвертация `.docx` в HTML через mammoth.js. Использует кастомный `defaultStyleMap` (Heading 1/2/3, русские «Заголовок 1/2/3», Title/Subtitle, Strong/Emphasis, Quote). Возвращает `{html, messages}`. Бросает `DocxParseError` при пустом буфере или ошибке парсинга. Рендеринг/preview больше не опираются на этот HTML (форматирование владеет оригинальный `.docx` через `buildDocxFromTemplate`); `parseDocxToHtml` вызывается только при загрузке шаблона для заполнения колонки `Template.contentHtml`, по которой `generationPipeline` эвристически определяет необходимость подгрузки товаров/картинок. |
| [`apps/backend/src/services/formulaEngine.ts`](./apps/backend/src/services/formulaEngine.ts) | Sandboxed mathjs-движок для формул шаблонов (Фаза 4). Приватная инстанция `math = create(all)` с заблокированными небезопасными функциями (`import`, `createUnit`, `simplify`, `derivative`, `rationalize`, `resolve`, `symbolicEqual`, `reviver`, `replacer`). Регистрирует helper-функции: `if(cond, a, b)`, `concat(...args)`, `format(value, pattern)` (0/0.00/0.0%/money/usd/eur), `dateFormat(date, fmt)` (iso/date/datetime/dd.MM.yyyy с токенами yyyy/yy/MM/dd/HH/mm/ss), `upper(s)`, `lower(s)`. Product-хелперы (инжектируются как замыкания в scope `evaluateExpression`): `productCount()` (количество товаров), `productSum("FIELD")` (сумма числового поля), `productGet(index, "FIELD")` (доступ к товару, 1-based), `productImage(index)` (base64-картинка товара). `collectDeps` обнаруживает product-хелперы через FunctionNode в AST и выставляет `deps.products = true`. `PRODUCT_HELPER_NAMES` — набор имён хелперов для AST-детекции. PRODUCTS НЕ в ENTITY_SYMBOLS (массив, не Record). Публичный API: `validateExpression(expression)` (возвращает `{ok, error?, deps?}`), `evaluateExpression(expression, context)` (возвращает `{value, raw, error?}` с контекстом `{DEAL, CONTACT, COMPANY, PRODUCTS}`), `extractDependencies(expression)` (статическая выборка `FormulaDependencies` через AccessorNode обход AST). Также экспортирует `formatNumber` и `formatDate` как отдельные утилиты. |
| [`apps/backend/src/services/dealData.ts`](./apps/backend/src/services/dealData.ts) | Сервис сборки контекста сделки (Фаза 5). `getDealContext(client, dealId, options?: GetDealContextOptions): Promise<FormulaContext>` — трёхступенчатый batch: 1) `crm.deal.get` + `crm.deal.contact.items.get` + опционально `crm.deal.productrows.get` (при `fetchProducts=true`), 2) `crm.contact.get` + `crm.company.get` (только если есть соответствующие id), 3) при `fetchProductImages=true` — загрузка картинок товаров через `getProductImages` + `downloadFileAsBase64` с конкурентностью ~5. Через `flattenEntity`/`flattenValue` нормализует мульти-значения (PHONE/EMAIL/WEB/IM как массивы `{VALUE,...}`) в скаляр (первый VALUE). Возвращает плоский `{DEAL, CONTACT, COMPANY, PRODUCTS}`. Интерфейс `GetDealContextOptions` (`fetchProducts?`, `fetchProductImages?`). Приватные хелперы: `normalizeProductRow`, `attachProductImages` (с `IMAGE_CONCURRENCY=5`). Бросает `DealDataError(message, code, status)` если deal не найден. Экспортирует `__internal` для тестов. |
| [`apps/backend/src/services/docxTemplateEngine.ts`](./apps/backend/src/services/docxTemplateEngine.ts) | Сервис прямой подстановки плейсхолдеров в .docx шаблоны через docxtemplater (без HTML промежуточного шага) — основной генератор документов пайплайна (вызывается из `generationPipeline.runGeneration`). `buildDocxFromTemplate(originalDocx: Buffer, options: BuildFromTemplateOptions): Promise<Buffer>` — открывает архив через PizZip, строит data object из formula results (tagKey → value, на ошибке — label) и значений ручных полей (`fieldValues[fieldKey]` по тем же делимитерам `{fieldKey}`), маппит ProductRow[] в массив `products` для `{#products}...{/products}` циклов, подключает docxtemplater-image-module-free для `{%TAG}` image плейсхолдеров (data:image/ base64 URI). Конфигурация: `paragraphLoop: true`, `linebreaks: true`, `delimiters: { start: '{', end: '}' }`, `nullGetter` возвращает пустую строку. `scanDocxPlaceholders(originalDocx: Buffer): string[]` — извлекает все имена тегов из .docx через InspectModule (`docxtemplater/js/inspect-module.js`), рекурсивно обходит вложенные теги циклов, возвращает отсортированный уникальный массив. На пустом/некорректном буфере возвращает `[]`. `DocxTemplateError` — класс ошибок с `code` (EMPTY_DOCX, ZIP_FAILED, COMPILE_FAILED, RENDER_FAILED, GENERATE_FAILED). `BuildFromTemplateOptions`: `formulas: Record<string, FormulaEvaluationResult>`, `products?: ProductRow[]`, `fieldValues?: Record<string, string>` (значения ручных полей по `fieldKey`), `title?: string`. Приватный `buildDataObject(formulas, products, fieldValues={})` сначала раскладывает ручные поля (`data[fieldKey]=value`), затем формулы (`data[tagKey]=value`) — на коллизии ключей формула ВЫИГРЫВАЕТ (ручные поля дополняют, не перетирают). Прочие приватные хелперы: `base64DataURLToArrayBuffer`, `createImageModule`, `collectTagNames`. |
| [`apps/backend/src/services/docxTemplateEngine.test.ts`](./apps/backend/src/services/docxTemplateEngine.test.ts) | Юнит-тесты `buildDocxFromTemplate` на встроенном раннере `node:test` (запуск через `tsx`, скрипт `pnpm -F backend test`). Собирает самодостаточную .docx-фикстуру в памяти через PizZip (валидный OOXML-ZIP: `[Content_Types].xml`, `_rels/.rels`, `word/document.xml` с плейсхолдерами `{formulaTag}`, `{manualField}`, циклом `{#products}{PRODUCT_NAME} — {SUM}{/products}` и форматированием `w:jc`/`w:ind`). Проверяет: 1) замену формулы и ручного поля + рендер цикла товаров + сохранность `w:jc`/`w:ind`, 2) приоритет формулы над ручным полем при коллизии ключа, 3) негативный кейс — пустой буфер бросает `DocxTemplateError('EMPTY_DOCX')`. Хелперы фикстуры: `buildFixtureDocx`, `readDocumentXml`, `formula`, `product`. |
| [`apps/backend/src/services/pdfBuilder.ts`](./apps/backend/src/services/pdfBuilder.ts) | **@deprecated (legacy, мёртвый код).** Сервис генерации PDF из HTML через Puppeteer (headless Chrome). Основной пайплайн генерации и preview переключены на `docxTemplateEngine.buildDocxFromTemplate` (.docx без HTML-шага), поэтому `runGeneration`/`routes/generate.ts`/preview его больше не вызывают; не импортируется ни одним модулем активного пути. Сохранён только для справки и диагностики, помечен на удаление в отдельной задаче. `buildPdfFromHtml(html, {formulas?, title?, products?, fieldValues?}): Promise<Buffer>`. Pipeline: 1) `expandProductTables(html, products)` (из `docxBuilder`), 2) `stripFormulaTags(html, formulas)` — подстановка computed values вместо `<span data-formula-key>` (img для `data:image/`), 3) `stripManualFieldTags(html, fieldValues)` — замена `<span data-field-key="K">` на значение `fieldValues[K]` (trim краёв, escape, `\n`→`<br>`), пустое если значения нет; в итоговом тексте `<span>` убирается, поэтому значение наследует шрифт/размер абзаца, 4) `wrapAsStyledHtml` — print-ready HTML5 (Times New Roman, A4), 5) рендер через синглтон-браузер (`getBrowser`), ожидание загрузки `<img>`, `page.pdf({format:'A4', margins})`. `closePdfBrowser()` закрывает браузер при остановке сервера. `BuildPdfOptions`: `formulas?`, `title?`, `products?`, `fieldValues?`. Бросает `PdfBuildError` с `code`. |
| [`apps/backend/src/services/docxBuilder.ts`](./apps/backend/src/services/docxBuilder.ts) | **@deprecated (legacy, мёртвый код).** Сервис генерации `.docx` из HTML. Активный пайплайн рендерит напрямую из оригинального `.docx` через `buildDocxFromTemplate`, поэтому `buildDocxFromHtml`/`expandProductTables` не вызываются из активного пути (импорт `expandProductTables` удалён из `templates.ts`). Помечен на удаление в отдельной задаче. `buildDocxFromHtml(html, {formulas?, title?, products?}): Promise<Buffer>` через `@turbodocx/html-to-docx`. Pipeline: 1) `expandProductTables(html, products)` — раскрытие `<table data-product-table="true">`: берёт последний `<tr>` в `<tbody>` как шаблонную строку, клонирует N раз по числу товаров, заменяет плейсхолдеры `data-product-field` (значение поля), `data-product-image` (`<img>` с base64), `data-product-index` (порядковый номер); при пустом массиве удаляет шаблонную строку. Хелпер `fillProductRow(rowHtml, product, index)` подставляет значения конкретного товара. 2) `stripFormulaTags` regex-заменой подставляет computed values вместо `<span data-formula-key>` (на ошибке — label, без формулы — снимает «Σ »); если `result.value` начинается с `data:image/`, генерирует `<img>` tag вместо text escape (для standalone `productImage()`). 3) `wrapAsHtmlDocument` — HTML5 shell с `lang=ru` UTF-8. Параметры html-to-docx: portrait, Arial, 11pt, A4 margins, table borders. `BuildDocxOptions`: `formulas?`, `title?`, `products?: ProductRow[]`. `expandProductTables` переиспользуется в `pdfBuilder`. Бросает `DocxBuildError`. Экспортирует `__internal` для тестов (`stripFormulaTags`, `wrapAsHtmlDocument`, `expandProductTables`, `fillProductRow`). |
| [`apps/backend/src/services/portalAuth.ts`](./apps/backend/src/services/portalAuth.ts) | Хелпер управления OAuth-токенами портала, хранящимися в `AppSettings` (id=1). `savePortalTokens({accessToken, refreshToken, expiresAt, memberId, domain, applicationToken?})` шифрует `accessToken`/`refreshToken`/`applicationToken` через `tokenCrypto.encryptToken` и делает upsert. `getFreshPortalAuth()` читает строку, проверяет expiry с запасом `REFRESH_SKEW_SECONDS=60`, возвращает расшифрованный `{accessToken, domain, memberId}` или вызывает `refreshPortalTokens`. `refreshPortalTokens(row)` расшифровывает `refreshToken`, ходит в `https://oauth.bitrix.info/oauth/token/` с `B24_CLIENT_ID`/`B24_CLIENT_SECRET`, парсит ответ и перезаписывает токены (повторно шифрует через `savePortalTokens`). Класс ошибок `PortalAuthError` с `kind: not_installed\|missing_tokens\|missing_client_credentials\|refresh_failed`. |
| [`apps/backend/src/services/tokenCrypto.ts`](./apps/backend/src/services/tokenCrypto.ts) | Симметричное шифрование токенов портала для хранения в БД. AES-256-GCM через `node:crypto` (IV 96 бит, authTag). Ключ берётся из `TOKEN_ENCRYPTION_KEY` (32 байта в base64/hex), иначе детерминированно выводится HKDF-SHA256 из `B24_APP_SECRET` с info-строкой `b24-doc-gen:token-encryption:v1` — работает на существующих установках без новой конфигурации. Формат хранения: `enc:v1:<ivB64>:<tagB64>:<cipherB64>` с префиксом, который делает `encryptToken`/`decryptToken` идемпотентными: двойное шифрование невозможно, legacy plaintext без префикса возвращается как есть (бесшовная миграция — старые записи перешифровываются при следующем upsert). Экспортирует `encryptToken(plain)`, `decryptToken(stored)`, `__resetTokenCryptoCacheForTests()`. |
| [`apps/backend/src/prisma/client.ts`](./apps/backend/src/prisma/client.ts) | Экспорт singleton-экземпляра `PrismaClient` с защитой от утечек при hot-reload (кеш на `globalThis.__prismaClient`). |

#### apps/backend/scripts

| Файл | Назначение |
|---|---|
| [`apps/backend/scripts/render-check.ts`](./apps/backend/scripts/render-check.ts) | **Legacy-диагностика** (запуск через `tsx`). Прогоняет все `.docx` из заданной папки через устаревший HTML/PDF-пайплайн `parseDocxToHtml` (mammoth) → HTML → `buildPdfFromHtml` (Puppeteer) → PDF, снимает по два скриншота и сохраняет `.pdf` + `_report.json` в подпапку `_render-check`. Относится к deprecated-движку (`docxParser`/`pdfBuilder`), не к активному `.docx`-пайплайну (`buildDocxFromTemplate`). Оставлен для визуальной отладки legacy-рендера. |
| [`apps/backend/scripts/verify-pvu-template.ts`](./apps/backend/scripts/verify-pvu-template.ts) | E2E-скрипт верификации активного пайплайна без Bitrix24 (запуск через `tsx`). Берёт реальный шаблон `первичное заявление в СК ПВУ.docx`, программно через PizZip делает размеченную копию (заменяет заглушки на `{tagKey}`/`{fieldKey}` в абзацах с выравниванием right/both и отступами `w:ind`), вызывает боевой `buildDocxFromTemplate` с моковыми formula results / fieldValues / products, распаковывает `document.xml` результата и проверяет: (а) плейсхолдеры заменены значениями (нет остаточных `{tag}`), (б) форматирование сохранено — счётчики `w:jc` (right/center/both) и `w:ind` в результате не меньше оригинала. Печатает отчёт PASS/FAIL, код выхода 1 при провале. Промежуточные `.docx` (`_pvu-marked`/`_pvu-rendered`) — временные. |

#### apps/backend/prisma

| Файл | Назначение |
|---|---|
| [`apps/backend/prisma/schema.prisma`](./apps/backend/prisma/schema.prisma) | Схема БД. Модели: `AppSettings`, `Theme`, `Template`, `Formula`, `TemplateField`, `Webhook`. Datasource `sqlite` → `file:./dev.db`. |
| [`apps/backend/prisma/migrations/`](./apps/backend/prisma/migrations/) | Миграции Prisma. `*_init/` — 4 базовые модели; `*_theme_settings/` — настройки темы; `*_add_webhook/` — таблица `Webhook` с FK на `Theme`/`Template`; `20260408172111_add_application_token/` — добавлено поле `AppSettings.applicationToken` для валидации callback'ов от Bitrix bizproc-робота / outgoing webhook. |

**Модели Prisma** (подробнее см. [`schema.prisma`](./apps/backend/prisma/schema.prisma)):

- **AppSettings** — `id` (Int @default(1)), `portalDomain`, `adminUserIds` (JSON-строка), `dealFieldBinding?`, `applicationToken?` (shared secret, полученный от Bitrix на install; используется `routes/webhookRun.ts` для аутентификации входящих вызовов робота), `installedAt`. Singleton-настройки приложения.
- **Theme** — `id` (cuid), `name`, `order`, `addToTimeline`, `dealFieldBinding?`, `templates[]`, `webhooks[]`, `createdAt`, `updatedAt`. Группировка шаблонов.
- **Template** — `id` (cuid), `name`, `themeId` → Theme, `contentHtml` (TipTap HTML), `originalDocx?` (Bytes), `formulas[]`, `fields[]`, `webhooks[]`. Индекс по `themeId`.
- **Formula** — `id` (cuid), `templateId` → Template (onDelete: Cascade), `tagKey`, `label`, `expression`, `dependsOn` (JSON-строка). Unique `(templateId, tagKey)`.
- **TemplateField** — `id` (cuid), `templateId` → Template (onDelete: Cascade), `fieldKey`, `label`, `type` (`text`/`textarea`/`number`/`date`, default `text`), `required` (default false), `placeholder?`, `defaultValue?` (значение по умолчанию: для `date` — токен `"today"` (текущая дата); для `text`/`textarea`/`number` — литеральное значение, которое пользователь может отредактировать при генерации), `order` (default 0). Unique `(templateId, fieldKey)`, индекс по `templateId`. Ручное поле, заполняемое пользователем при генерации; ссылается из `contentHtml` нодой `manualFieldTag` (`<span data-field-key>`).
- **Webhook** — `id` (cuid), `token` (unique, URL-safe cryptorandom), `scope` (`"theme"`/`"template"`), `themeId?` → Theme (onDelete: Cascade), `templateId?` → Template (onDelete: Cascade), `label?`, `enabled` (default true), `createdAt`, `lastUsedAt?`, `useCount` (default 0). Индексы по `themeId` и `templateId`. Используется публичным endpoint'ом `POST /api/webhook/run/:token` для запуска генерации документов по сигналу от робота «Исходящий вебхук» Bitrix24.

## Поток данных (высокоуровневый)

1. **Установка:** фронтенд детектит отсутствие `AppSettings` → `InstallPage` → `POST /api/install` → backend сохраняет настройки.
2. **Загрузка шаблона:** админ загружает `.docx` с плейсхолдерами `{tag}` → backend сохраняет `originalDocx` и сканирует теги (`scanDocxPlaceholders`).
3. **Разметка тегов (`TemplateEditorPage`):** редактор грузит шаблон с `originalDocxBase64` + `docxPlaceholders`, слева — РЕДАКТИРУЕМЫЙ `.docx` через `<DocxEditor>` (`@eigenpal/docx-editor-react`), справа — панель «Теги шаблона». Для каждого тега админ привязывает формулу (`FormulaBuilder` + `FieldPicker` → валидация mathjs, `tagKey` = имя тега) или объявляет ручным полем (`ManualFieldBuilder`, `fieldKey` = имя тега). Продуктовые теги цикла зарезервированы. Сохранение (`handleSave`): если документ менялся — отредактированный `.docx` сначала уходит в `PUT /api/templates/:id/docx` (замена `originalDocx` + пересканирование тегов), затем привязки → `PUT /api/templates/:id` с `formulas[]`/`fields[]` (без `contentHtml`-разметки). Новые несвязанные теги из отредактированного `.docx` появляются в панели после сохранения.
4. **Генерация:** `GeneratePage` → `POST /api/templates/:id/preview { dealId, fieldValues? }` → backend собирает данные (`dealData`), вычисляет формулы, разрешает ручные поля и подставляет всё прямо в оригинальный `.docx` через `docxTemplateEngine.buildDocxFromTemplate` → ответ `{ docxBase64, tags, formulas, fields }` (предпросмотр `.docx` рендерится на клиенте через `docx-preview` `renderAsync`); пользователь заполняет ручные поля (форма + debounce-перезапрос preview) → `POST /api/generate { fieldValues }` → `runGeneration` → `docxTemplateEngine.buildDocxFromTemplate(template.originalDocx, {formulas, products, fieldValues})` (прямая подстановка в оригинальный `.docx`, без HTML→PDF) → `disk.folder.uploadfile` (`.docx`) → `crm.item.update` → `crm.timeline.comment.add`.

## Роли и безопасность

Приложение различает две роли:

- **admin** — Bitrix24-пользователь, чей `userId` присутствует в
  `AppSettings.adminUserIds`. Может создавать/редактировать/удалять
  темы, шаблоны, формулы и менять настройки.
- **user** — любой другой пользователь портала, имеющий доступ к
  iframe приложения. Может только генерировать документы по готовым
  шаблонам в карточке сделки.

### Backend

- Глобальный `registerAuthMiddleware` гейтит все `/api/*` маршруты,
  валидирует Bitrix24-headers и заполняет `request.b24Auth`.
- `requireAdmin` (см. [`apps/backend/src/middleware/role.ts`](./apps/backend/src/middleware/role.ts))
  применяется как `preHandler` на ВСЕХ mutation-роутах:
  - POST/PUT/DELETE `/api/themes(/:id)`
  - POST `/api/templates`, POST `/api/templates/upload`,
    PUT/DELETE `/api/templates/:id`
  - PUT `/api/settings`, POST `/api/settings/create-field`
- `loadCurrentRole(request)` — резолвит роль без 403, используется
  `GET /api/me` для отдачи `{userId, role}` фронтенду.
- `invalidateRoleCache()` зовётся из `routes/install.ts` после первой
  установки и из `routes/settings.ts` после смены `adminUserIds`,
  чтобы закешированный набор админ-id не отставал от БД.
- Централизованный `setErrorHandler` в [`server.ts`](./apps/backend/src/server.ts)
  возвращает все ошибки в формате `{error: {code, message, details?}}`,
  логгирует 5xx через `request.log.error` со stack-трейсом, а в dev
  режиме включает stack в `details`.

### Frontend

- [`useCurrentRole()`](./apps/frontend/src/lib/useCurrentRole.ts) —
  TanStack Query поверх `meApi.get()`, кешируется на сессию
  (`staleTime: Infinity`). Возвращает `{role, isAdmin, isLoading, ...}`.
- [`<AdminOnly>`](./apps/frontend/src/components/AdminOnly.tsx) —
  обёртка, рендерит детей только для админа. Используется в
  `TemplatesPage` и `ThemeSidebar` чтобы скрыть «Загрузить шаблон»,
  «Создать тему», «Переименовать», «Удалить», «Открыть редактор».
- [`PlacementGuard`](./apps/frontend/src/components/PlacementGuard.tsx)
  после установки приложения резолвит роль и для любого пути под
  `/settings` отдаёт 403-stub если пользователь не админ — это страховка
  на случай, если кто-то откроет URL вручную минуя UI-кнопки.
- Все ошибки от `apiRequest`/`uploadRequest` пробрасываются в UI как
  `ApiError` (см. [`apps/frontend/src/lib/api.ts`](./apps/frontend/src/lib/api.ts));
  для единообразного показа toast-ов есть хук
  [`useApiError()`](./apps/frontend/src/lib/useApiError.ts) и
  imperative-функция `reportApiError(err, fallbackTitle)`. Глобальный
  `<Toaster>` смонтирован в [`App.tsx`](./apps/frontend/src/App.tsx).

## Workspace команды

Из корня проекта:

```bash
pnpm install              # установить всё
pnpm dev                  # параллельный dev (frontend + backend)
pnpm dev:frontend         # только Vite
pnpm dev:backend          # только Fastify
pnpm build                # сборка всех пакетов
pnpm typecheck            # tsc --noEmit по всем
pnpm -F backend db:push   # синхронизация Prisma-схемы с SQLite
pnpm -F backend db:migrate # создание миграции
```
