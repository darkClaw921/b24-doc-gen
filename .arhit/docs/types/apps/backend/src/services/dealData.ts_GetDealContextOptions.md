# apps/backend/src/services/dealData.ts:GetDealContextOptions

Интерфейс опций для getDealContext. fetchProducts?: boolean — загружать ли товарные позиции через crm.deal.productrows.get. fetchProductImages?: boolean — загружать ли картинки товаров из catalog.productImage.list (работает только совместно с fetchProducts=true).
