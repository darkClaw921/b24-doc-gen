# apps/frontend/src/pages/SettingsPage.tsx

Страница настроек админа. Секции: 1) Поле для файлов — выбор UF_CRM_* поля типа file для привязки .docx, создание нового поля. 2) Администраторы — поиск пользователей и управление списком админов. 3) Места встройки — список зарегистрированных placement-обработчиков приложения (через GET /api/placements) с возможностью удаления каждого через DELETE /api/placements (placement.unbind). Удаление с диалогом подтверждения.
