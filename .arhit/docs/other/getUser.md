# getUser

Метод B24Client: user.get с фильтром {ID}. Возвращает первую запись пользователя или null. Используется для резолва ASSIGNED_BY_ID сделки в контекст ASSIGNED при вычислении формул (dealData.getDealContext и formulas.fetchDealContext).
